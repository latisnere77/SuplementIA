# Portal API Configuration (DISABLED = use internal system instead of external backend)
PORTAL_API_URL=DISABLED

# App URL (auto-detected in production, set for local development)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Other environment variables
NODE_ENV=development
