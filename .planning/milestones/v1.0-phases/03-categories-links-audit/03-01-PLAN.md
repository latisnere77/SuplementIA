---
phase: 03-categories-links-audit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/__tests__/categories-links-audit.test.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - LINK-01
  - LINK-02
must_haves:
  truths:
    - "Test file exists with describe blocks for CAT-01, CAT-02, LINK-01, LINK-02"
    - "All tests are failing (RED) — no fixes applied yet"
    - "6 KNOWN_MISSING slugs documented with it.todo"
    - "Running the test file produces failures, not an error/crash"
  artifacts:
    - path: "lib/__tests__/categories-links-audit.test.ts"
      provides: "Failing test stubs for all 4 phase requirements"
      exports: []
  key_links:
    - from: "lib/__tests__/categories-links-audit.test.ts"
      to: "lib/knowledge-base.ts"
      via: "import getAllCategories"
      pattern: "getAllCategories"
    - from: "lib/__tests__/categories-links-audit.test.ts"
      to: "lib/portal/supplements-database.ts"
      via: "import SUPPLEMENTS_DATABASE"
      pattern: "SUPPLEMENTS_DATABASE"
---

<objective>
Create the Wave 0 test file with failing stubs for all four phase requirements (CAT-01, CAT-02, LINK-01, LINK-02). No source fixes are applied in this plan — the tests must be RED to confirm they detect the real problems before any fix is written.

Purpose: Establishes the Nyquist-compliant feedback loop for the phase. Subsequent plans turn these tests GREEN.
Output: lib/__tests__/categories-links-audit.test.ts with 4 describe blocks, all failing.
</objective>

<execution_context>
@/Users/latisnere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/latisnere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-categories-links-audit/03-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From lib/knowledge-base.ts:
```typescript
export type EvidenceGrade = 'A' | 'B' | 'C' | 'D' | 'F';

export interface SupplementEvidence {
  name: string;
  evidenceGrade: EvidenceGrade;
  summary: string;
  slug: string; // e.g., 'melatonin'
}

export interface HealthCategory {
  slug: string; // e.g., 'sleep'
  name: string;
  description: string;
  supplements: SupplementEvidence[];
}

export const getAllCategories = (): HealthCategory[] => { ... }
export const getCategoryBySlug = (slug: string): HealthCategory | undefined => { ... }
```

From lib/portal/supplements-database.ts:
```typescript
export const SUPPLEMENTS_DATABASE: Array<{ id: string; ... }>
// IDs follow the pattern: 'melatonin', 'chamomile-es', 'chamomile-en', etc.
// Base ID = entry.id.replace(/-(?:es|en)$/, '')
```

Jest config (jest.config.js): uses @swc/jest transform, moduleNameMapper for @/ alias.
Test file location convention (Phase 2): lib/__tests__/  — use /** @jest-environment node */ at top.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing test stubs for CAT-01, CAT-02, LINK-01, LINK-02</name>
  <files>lib/__tests__/categories-links-audit.test.ts</files>
  <behavior>
    - CAT-01 describe: for each non-KNOWN_MISSING supplement slug in knowledge-base, expects baseIds.has(slug) to be true. All 5 slug mismatches (rhodiola-rosea, whey-protein, omega-3, ginkgo-biloba, hydrolyzed-collagen) must FAIL.
    - CAT-01 KNOWN_MISSING set: lavender, caffeine, beta-alanine, bacopa-monnieri, fiber-psyllium, echinacea — marked with it.todo, not skipped silently.
    - LINK-01 describe: 3 tests — (a) category page content does NOT match /href=["']\/portal\/search["']/ (must FAIL before fix), (b) GuidesCategories content does NOT match /['"]\/portal\/categories['"]/ (must FAIL before fix), (c) scan of app/ and components/ dirs finds no /portal/search refs (must FAIL before fix).
    - LINK-02 describe: 2 tests — (a) supplement detail page does NOT have bare unguarded href={`/portal/category/${benefit}`} (must FAIL before fix), (b) category page contains notFound() call (must PASS — already exists; this is a verification).
    - CAT-02 describe: 1 test — verify category page contains notFound() call (same as LINK-02 test b — can be a separate describe or combined; use CAT-02 label).
  </behavior>
  <action>
    Write lib/__tests__/categories-links-audit.test.ts with the following structure:

    1. Header: /** @jest-environment node */
    2. Imports: fs, path (built-in), getAllCategories from ../../lib/knowledge-base, SUPPLEMENTS_DATABASE from ../../lib/portal/supplements-database
    3. Build baseIds Set: new Set(SUPPLEMENTS_DATABASE.map(e => e.id.replace(/-(?:es|en)$/, '')))
    4. Define KNOWN_MISSING Set with exactly these 6 slugs: lavender, caffeine, beta-alanine, bacopa-monnieri, fiber-psyllium, echinacea
    5. CAT-01 describe block: iterate getAllCategories(), for each category iterate supplements. If slug in KNOWN_MISSING → it.todo with message "KNOWN GAP: not in SUPPLEMENTS_DATABASE". Otherwise → it(...) { expect(baseIds.has(supplement.slug)).toBe(true) }. Test names include slug and category slug for traceability.
    6. LINK-01 describe block with 3 it() cases using fs.readFileSync for files (a) and (b), and recursive directory scan for (c). All three assertions are negations of the broken state — they will FAIL before the fix.
    7. LINK-02 describe block with 2 it() cases: (a) supplement detail page back-link null-check assertion (FAILS before fix), (b) category page has notFound() call (PASSES — verification).
    8. CAT-02 describe block with 1 it() case: category page calls notFound() for unknown slugs (same file-content read as LINK-02 test b — PASSES).

    File paths in readFileSync must be relative to the project root (Jest runs from root):
    - 'app/[locale]/portal/category/[slug]/page.tsx'
    - 'components/portal/GuidesCategories.tsx'
    - 'app/[locale]/portal/supplement/[slug]/page.tsx'

    Use exact code patterns from 03-RESEARCH.md sections "CAT-01: Slug validation test structure", "LINK-01: Static href assertion test", and "LINK-02: Null-check back-link test" as the implementation reference.

    Do NOT run any fixes. This plan's only output is the test file.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/categories-links-audit.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    Test file runs without a crash. CAT-01 tests fail for 5 slug mismatches (rhodiola-rosea, whey-protein, omega-3, ginkgo-biloba, hydrolyzed-collagen). LINK-01 tests fail for the 3 broken hrefs. LINK-02 test (a) fails for unguarded benefit href. LINK-02 test (b) and CAT-02 tests pass (notFound already present). 6 KNOWN_MISSING slugs show as todo.
  </done>
</task>

</tasks>

<verification>
Run: npx jest lib/__tests__/categories-links-audit.test.ts --no-coverage

Expected outcome: test suite runs (no crash/import error), multiple failures visible for slug mismatches and broken hrefs, it.todo entries listed for the 6 KNOWN_MISSING slugs.
</verification>

<success_criteria>
- lib/__tests__/categories-links-audit.test.ts exists
- Test file imports from knowledge-base and supplements-database without error
- CAT-01 block: 5 failing tests (slug mismatches), 6 todos (KNOWN_MISSING)
- LINK-01 block: 3 failing tests (broken hrefs)
- LINK-02 block: 1 failing test (unguarded href), 1 passing test (notFound present)
- CAT-02 block: 1 passing test (notFound present)
- No test crashes or import errors
</success_criteria>

<output>
After completion, create .planning/phases/03-categories-links-audit/03-01-SUMMARY.md
</output>
