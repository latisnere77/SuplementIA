---
phase: 03-categories-links-audit
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - lib/knowledge-base.ts
autonomous: true
requirements:
  - CAT-01
must_haves:
  truths:
    - "All 5 slug mismatches in knowledge-base.ts are corrected to match SUPPLEMENTS_DATABASE base IDs"
    - "6 KNOWN_MISSING slugs remain in knowledge-base (no data added, flagging via test is sufficient)"
    - "CAT-01 tests pass (only todos remain for KNOWN_MISSING)"
    - "No existing category page behavior is broken"
  artifacts:
    - path: "lib/knowledge-base.ts"
      provides: "Corrected supplement slugs aligned with SUPPLEMENTS_DATABASE base IDs"
      contains: "slug: 'rhodiola'"
  key_links:
    - from: "lib/knowledge-base.ts"
      to: "lib/portal/supplements-database.ts"
      via: "supplement slug must equal base ID derived from SUPPLEMENTS_DATABASE entry"
      pattern: "slug: 'rhodiola'"
---

<objective>
Fix 5 slug mismatches in lib/knowledge-base.ts so every supplement slug that has a backing entry in SUPPLEMENTS_DATABASE uses the correct base ID. The 6 supplements with no DB entry at all are left untouched (flagged by test todos).

Purpose: Satisfies CAT-01 — knowledge-base supplement slugs now resolve to real SUPPLEMENTS_DATABASE entries.
Output: lib/knowledge-base.ts with corrected slugs; CAT-01 tests green.
</objective>

<execution_context>
@/Users/latisnere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/latisnere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-categories-links-audit/03-01-SUMMARY.md
</context>

<interfaces>
<!-- The 5 exact slug changes required. No exploration needed. -->

Slug corrections (knowledge-base.ts slug → correct DB base ID):
1. 'rhodiola-rosea' → 'rhodiola'       (energy category, Rhodiola Rosea)
2. 'whey-protein'   → 'protein'        (muscle-gain category, Proteína de Suero)
3. 'omega-3'        → 'omega3'         (cognitive-function AND heart-health categories — both occurrences)
4. 'ginkgo-biloba'  → 'ginkgo'         (cognitive-function category, Ginkgo Biloba)
5. 'hydrolyzed-collagen' → 'collagen'  (joint-bone-health category, Colágeno Hidrolizado)

Note: omega-3 appears TWICE in knowledge-base.ts (once in cognitive-function, once in heart-health). Both must be changed.
Total: 6 slug value changes across 5 distinct supplement entries.

Slugs to leave untouched (KNOWN_MISSING — no DB entry):
- 'lavender', 'caffeine', 'beta-alanine', 'bacopa-monnieri', 'fiber-psyllium', 'echinacea'

From lib/knowledge-base.ts:
```typescript
export interface SupplementEvidence {
  slug: string; // This is the field to change — only the value, not the field name
}

export const getAllCategories = (): HealthCategory[] => { ... }
export const getCategoryBySlug = (slug: string): HealthCategory | undefined => { ... }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix 5 slug mismatches in knowledge-base.ts</name>
  <files>lib/knowledge-base.ts</files>
  <behavior>
    - After edit: getAllCategories() returns no supplement with slug 'rhodiola-rosea' — returns 'rhodiola' instead
    - After edit: no supplement slug 'whey-protein' — 'protein' instead
    - After edit: no supplement slug 'omega-3' — 'omega3' in both cognitive-function and heart-health categories
    - After edit: no supplement slug 'ginkgo-biloba' — 'ginkgo' instead
    - After edit: no supplement slug 'hydrolyzed-collagen' — 'collagen' instead
    - KNOWN_MISSING slugs (lavender, caffeine, beta-alanine, bacopa-monnieri, fiber-psyllium, echinacea) are unchanged
    - No other fields (name, evidenceGrade, summary, category slug) are modified
  </behavior>
  <action>
    Read lib/knowledge-base.ts first. Apply exactly these 6 targeted slug value changes (change only the string value of the `slug` field, nothing else):

    1. Find: slug: 'rhodiola-rosea'  →  Change to: slug: 'rhodiola'
    2. Find: slug: 'whey-protein'    →  Change to: slug: 'protein'
    3. Find: slug: 'omega-3'         →  Change to: slug: 'omega3'   (first occurrence — cognitive-function category)
    4. Find: slug: 'omega-3'         →  Change to: slug: 'omega3'   (second occurrence — heart-health category)
    5. Find: slug: 'ginkgo-biloba'   →  Change to: slug: 'ginkgo'
    6. Find: slug: 'hydrolyzed-collagen'  →  Change to: slug: 'collagen'

    These are data edits, not logic changes. Use the Edit tool for each change, or read + write the full file. Do not alter any other content.

    After editing, confirm no remaining occurrences of the old slugs exist in the file.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/categories-links-audit.test.ts -t "CAT-01" --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>
    CAT-01 tests pass. Only it.todo entries remain for the 6 KNOWN_MISSING slugs. No test failures for slug mismatches. lib/knowledge-base.ts contains no occurrences of 'rhodiola-rosea', 'whey-protein', 'omega-3', 'ginkgo-biloba', or 'hydrolyzed-collagen' as slug values.
  </done>
</task>

</tasks>

<verification>
npx jest lib/__tests__/categories-links-audit.test.ts -t "CAT-01" --no-coverage

Expected: all CAT-01 it() tests pass, 6 todos listed for KNOWN_MISSING, zero failures.
</verification>

<success_criteria>
- lib/knowledge-base.ts updated: 5 slug fields corrected to match SUPPLEMENTS_DATABASE base IDs
- omega-3 corrected in both cognitive-function AND heart-health categories (2 edits)
- CAT-01 test suite: green (no failures, only 6 todos for KNOWN_MISSING)
- All other tests in the file remain unaffected
</success_criteria>

<output>
After completion, create .planning/phases/03-categories-links-audit/03-02-SUMMARY.md
</output>
