---
phase: 04-seo-analytics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/__tests__/sitemap.test.ts
  - components/portal/__tests__/seo-meta.test.ts
  - lib/__tests__/analytics-events.test.ts
  - lib/__tests__/gsc.test.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03

must_haves:
  truths:
    - "All four test files exist and fail RED before any implementation code is written"
    - "Test stubs import from files that do not yet exist (sitemap, results page wrapper)"
    - "Each test describes the exact contract the implementation must satisfy"
  artifacts:
    - path: "lib/__tests__/sitemap.test.ts"
      provides: "SEO-02 test stubs: URL count, locale coverage, domain prefix"
      exports: []
    - path: "components/portal/__tests__/seo-meta.test.ts"
      provides: "SEO-01 test stubs: unique title, locale-aware, OG tags, canonical URL"
      exports: []
    - path: "lib/__tests__/analytics-events.test.ts"
      provides: "SEO-03 test stubs: track() call assertions for all 3 custom events"
      exports: []
    - path: "lib/__tests__/gsc.test.ts"
      provides: "SEO-03 GSC stub: layout metadata verification.google field"
      exports: []
  key_links:
    - from: "lib/__tests__/sitemap.test.ts"
      to: "app/sitemap.ts"
      via: "import sitemap from '@/app/sitemap'"
      pattern: "from '@/app/sitemap'"
    - from: "components/portal/__tests__/seo-meta.test.ts"
      to: "app/[locale]/portal/results/page.tsx"
      via: "import { generateMetadata } from '@/app/[locale]/portal/results/page'"
      pattern: "from '@/app/\\[locale\\]/portal/results/page'"
---

<objective>
Create all four Wave 0 RED test stubs required by VALIDATION.md before any implementation code is written. Tests must import from files that do not yet exist and fail with import errors or explicit `it.todo` failures.

Purpose: Establish the RED state of TDD — tests define the contract that Wave 1 plans must satisfy.
Output: 4 test files, all failing, covering SEO-01, SEO-02, and SEO-03.
</objective>

<execution_context>
@/Users/latisnere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/latisnere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-analytics/04-VALIDATION.md
@.planning/phases/04-seo-analytics/04-RESEARCH.md

<interfaces>
<!-- Test framework: Jest 30 + @swc/jest, config at jest.config.js (root) -->
<!-- Quick run: jest --testPathPattern="seo-meta|sitemap|analytics-events|gsc" -->

From @vercel/analytics (browser track — used in client components only):
```typescript
import { track } from '@vercel/analytics';
track(eventName: string, properties?: Record<string, AllowedPropertyValues>): void
// AllowedPropertyValues = string | number | boolean | null | undefined
// Nested objects NOT allowed
```

VALIDATION.md defines these 4 test files:
- lib/__tests__/sitemap.test.ts         → SEO-02
- components/portal/__tests__/seo-meta.test.ts → SEO-01
- lib/__tests__/analytics-events.test.ts → SEO-03 (events)
- lib/__tests__/gsc.test.ts             → SEO-03 (GSC token)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sitemap + SEO-meta test stubs</name>
  <files>lib/__tests__/sitemap.test.ts, components/portal/__tests__/seo-meta.test.ts</files>
  <behavior>
    - sitemap test: returns exactly 308 URLs (2 index + 306 supplement)
    - sitemap test: 306 supplement URLs match `/portal/results?q=` pattern
    - sitemap test: both /es/ and /en/ variants exist for slug "ashwagandha"
    - sitemap test: every URL starts with https://suplementia.com/
    - sitemap test: 2 index URLs (/es/portal and /en/portal) included
    - seo-meta test: generateMetadata returns ES locale title containing "Evidencia Científica" for locale='es'
    - seo-meta test: generateMetadata returns EN locale title containing "Scientific Evidence" for locale='en'
    - seo-meta test: openGraph.title, openGraph.description, openGraph.url all defined
    - seo-meta test: alternates.canonical contains '/es/' for es locale, NOT '/en/'
    - seo-meta test: generateMetadata without ?q= param returns fallback title
  </behavior>
  <action>
Create `lib/__tests__/sitemap.test.ts` as a RED stub that imports from `@/app/sitemap` (which does not exist yet). Use the exact test structure from RESEARCH.md:

```typescript
// lib/__tests__/sitemap.test.ts
import sitemap from '@/app/sitemap';

describe('sitemap()', () => {
  it('returns exactly 308 URLs (2 index + 306 supplement)', () => {
    const urls = sitemap();
    expect(urls).toHaveLength(308);
  });

  it('contains 306 supplement URLs using /portal/results?q= pattern', () => {
    const supplementUrls = sitemap().filter((u: { url: string }) => u.url.includes('/portal/results'));
    expect(supplementUrls).toHaveLength(306);
  });

  it('has both /es/ and /en/ for ashwagandha', () => {
    const urls = sitemap().map((u: { url: string }) => u.url);
    expect(urls).toContain('https://suplementia.com/es/portal/results?q=ashwagandha');
    expect(urls).toContain('https://suplementia.com/en/portal/results?q=ashwagandha');
  });

  it('all URLs begin with https://suplementia.com/', () => {
    const urls = sitemap().map((u: { url: string }) => u.url);
    urls.forEach((url: string) => {
      expect(url).toMatch(/^https:\/\/suplementia\.com\//);
    });
  });

  it('includes /es/portal and /en/portal as index pages', () => {
    const urls = sitemap().map((u: { url: string }) => u.url);
    expect(urls).toContain('https://suplementia.com/es/portal');
    expect(urls).toContain('https://suplementia.com/en/portal');
  });
});
```

Create `components/portal/__tests__/seo-meta.test.ts` importing `generateMetadata` from `@/app/[locale]/portal/results/page` (which does not yet export it). The import will fail RED until Plan 02 creates the server wrapper:

```typescript
// components/portal/__tests__/seo-meta.test.ts
import { generateMetadata } from '@/app/[locale]/portal/results/page';

describe('generateMetadata — results page', () => {
  it('returns ES locale title containing "Evidencia Científica"', async () => {
    const meta = await generateMetadata({
      params: { locale: 'es' },
      searchParams: { q: 'ashwagandha' },
    });
    expect(String(meta.title)).toContain('Evidencia Científica');
  });

  it('returns EN locale title containing "Scientific Evidence"', async () => {
    const meta = await generateMetadata({
      params: { locale: 'en' },
      searchParams: { q: 'ashwagandha' },
    });
    expect(String(meta.title)).toContain('Scientific Evidence');
  });

  it('title includes the supplement name', async () => {
    const meta = await generateMetadata({
      params: { locale: 'es' },
      searchParams: { q: 'ashwagandha' },
    });
    expect(String(meta.title)).toMatch(/ashwagandha/i);
  });

  it('includes openGraph with title, description, and url', async () => {
    const meta = await generateMetadata({
      params: { locale: 'es' },
      searchParams: { q: 'ashwagandha' },
    });
    const og = meta.openGraph as Record<string, unknown>;
    expect(og).toBeDefined();
    expect(og.title).toBeTruthy();
    expect(og.description).toBeTruthy();
    expect(og.url).toBeTruthy();
  });

  it('canonical URL contains /es/ for es locale', async () => {
    const meta = await generateMetadata({
      params: { locale: 'es' },
      searchParams: { q: 'ashwagandha' },
    });
    const canonical = (meta.alternates as Record<string, string>)?.canonical;
    expect(canonical).toContain('/es/');
    expect(canonical).not.toContain('/en/');
  });

  it('returns fallback title when no q param', async () => {
    const meta = await generateMetadata({
      params: { locale: 'es' },
      searchParams: {},
    });
    expect(meta.title).toBeTruthy();
  });
});
```

Confirm `components/portal/__tests__/` directory exists (from Phase 3 — it does per git status). Run tests to confirm RED state.
  </action>
  <verify>
    <automated>cd /Users/latisnere/Developer/suplementAI && npx jest --testPathPattern="sitemap|seo-meta" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist. Tests fail with import/module errors (cannot find module '@/app/sitemap') or assertion failures — RED state confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Analytics events + GSC test stubs</name>
  <files>lib/__tests__/analytics-events.test.ts, lib/__tests__/gsc.test.ts</files>
  <behavior>
    - analytics-events test: track('search_submitted') called with {query, locale} after search submit
    - analytics-events test: track('supplement_view') called when results page mounts with data
    - analytics-events test: track('result_click') called on supplement card click
    - gsc test: layout metadata object has verification.google field that is non-empty
  </behavior>
  <action>
Create `lib/__tests__/analytics-events.test.ts`. These are stubs with `it.todo` — the full implementation requires rendering client components with complex mocks. Mark them as todo so they fail RED but do not block CI with complex setup:

```typescript
// lib/__tests__/analytics-events.test.ts
// Analytics event stubs — RED state until Plan 04 adds track() calls
// These will be replaced with full integration assertions post-implementation

jest.mock('@vercel/analytics', () => ({ track: jest.fn() }));

describe('Vercel Analytics custom events (SEO-03)', () => {
  it.todo('track("search_submitted") called with {query, locale} when portal search submitted');
  it.todo('track("supplement_view") called with {supplement, locale} when results page mounts with data');
  it.todo('track("result_click") called with {supplement, from} when supplement card clicked');
});
```

Create `lib/__tests__/gsc.test.ts`. Import the layout metadata export and assert on the `verification.google` field:

```typescript
// lib/__tests__/gsc.test.ts
// GSC verification token stub — RED until Plan 03 adds verification field to layout
import { metadata } from '@/app/[locale]/layout';

describe('GSC verification (SEO-03)', () => {
  it('layout metadata has non-empty verification.google field', () => {
    const verification = (metadata as Record<string, unknown>).verification as
      | { google?: string }
      | undefined;
    expect(verification).toBeDefined();
    expect(verification?.google).toBeTruthy();
    expect(verification?.google).not.toBe('');
    expect(verification?.google).not.toBe('PASTE_GSC_TOKEN_HERE');
  });
});
```

Note: `gsc.test.ts` will fail RED because `metadata` has no `verification` field yet AND because the token should not be the placeholder. After Plan 03, only the placeholder check fails — executor should update the test expectation to `not.toBe('')` only (accepting the placeholder string is valid for code-only scope).

Actually: Per CONTEXT.md, GSC is a code task only — the placeholder IS the deliverable. Adjust the gsc test to accept the placeholder as a valid non-empty value. Update the assertion to:
```typescript
expect(verification?.google).toBeTruthy(); // placeholder counts as truthy
```
Remove the `not.toBe('PASTE_GSC_TOKEN_HERE')` line so the test goes GREEN once the field exists with the placeholder. This is correct scope — actual token is an ops task.
  </action>
  <verify>
    <automated>cd /Users/latisnere/Developer/suplementAI && npx jest --testPathPattern="analytics-events|gsc" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist. analytics-events stubs show as todo (not failing, deferred). gsc.test.ts fails RED because layout metadata has no verification field yet.</done>
</task>

</tasks>

<verification>
Run all four test files to confirm RED/stub state:
```
cd /Users/latisnere/Developer/suplementAI && npx jest --testPathPattern="sitemap|seo-meta|analytics-events|gsc" --no-coverage 2>&1 | tail -30
```
Expected: sitemap and seo-meta fail with import errors; gsc fails with assertion; analytics-events shows todos. No Green tests from unimplemented code.
</verification>

<success_criteria>
- 4 test files exist in their respective directories
- sitemap.test.ts: fails RED (module not found for @/app/sitemap)
- seo-meta.test.ts: fails RED (generateMetadata not exported from results/page)
- analytics-events.test.ts: shows 3 todos (skipped, not broken)
- gsc.test.ts: fails RED (no verification.google in layout metadata)
- No implementation files created in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-analytics/04-01-SUMMARY.md`
</output>
