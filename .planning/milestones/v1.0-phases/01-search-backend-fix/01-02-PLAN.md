---
phase: 01-search-backend-fix
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/services/lexicon-generator.ts
  - lib/services/lexicon-generator.test.ts
  - lib/services/pubmed-search.ts
  - lib/services/__tests__/lexicon-coverage.test.ts
  - lib/services/__tests__/search-integration.test.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]

must_haves:
  truths:
    - "SUPPLEMENT_LEXICON contains all 158 supplements from SUPPLEMENTS_DATABASE"
    - "Each lexicon entry has Spanish name, English name, and alias variants"
    - "Quiz route (searchPubMed) can find any supplement from the database"
    - "Every supplement in SUPPLEMENTS_DATABASE has a matching SUPPLEMENT_LEXICON entry"
    - "No supplement from the database is silently dropped during lexicon generation"
  artifacts:
    - path: "lib/services/lexicon-generator.ts"
      provides: "Auto-generates SUPPLEMENT_LEXICON from SUPPLEMENTS_DATABASE"
      exports: ["generateLexicon", "GENERATED_LEXICON"]
    - path: "lib/services/pubmed-search.ts"
      provides: "Updated SUPPLEMENT_LEXICON with 158 supplements (was 10)"
      contains: "GENERATED_LEXICON"
    - path: "lib/services/__tests__/lexicon-coverage.test.ts"
      provides: "Exhaustive coverage test for all 158 supplements"
      min_lines: 80
    - path: "lib/services/__tests__/search-integration.test.ts"
      provides: "Integration test verifying search pipeline for quiz route"
      min_lines: 60
  key_links:
    - from: "lib/services/lexicon-generator.ts"
      to: "lib/portal/supplements-database.ts"
      via: "import SUPPLEMENTS_DATABASE"
      pattern: "import.*SUPPLEMENTS_DATABASE.*supplements-database"
    - from: "lib/services/pubmed-search.ts"
      to: "lib/services/lexicon-generator.ts"
      via: "import generated lexicon"
      pattern: "generateLexicon|GENERATED_LEXICON"
    - from: "lib/services/__tests__/lexicon-coverage.test.ts"
      to: "lib/services/lexicon-generator.ts"
      via: "import GENERATED_LEXICON"
      pattern: "import.*GENERATED_LEXICON"
---

<objective>
Expand SUPPLEMENT_LEXICON from 10 hardcoded entries to all 158 supplements (secondary fix for quiz/PubMed route) and prove 100% coverage with exhaustive tests.

Purpose: While Plan 01 fixes the main search flow (recommend -> enrich-v2 -> Lambda) by resolving Spanish->English names, this plan fixes the secondary flow: the quiz route uses searchPubMed which depends on SUPPLEMENT_LEXICON. The LEXICON only has 10 entries, causing the quiz route to silently miss 148 supplements. This plan also creates exhaustive tests proving both flows work.

Output: Lexicon generator, updated pubmed-search.ts, and comprehensive test suite.
</objective>

<execution_context>
@/Users/latisnere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/latisnere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/01-search-backend-fix/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From lib/portal/resolve-supplement-name.ts (created by Plan 01):
```typescript
export function resolveToEnglishName(query: string): string;
// Maps Spanish names/aliases -> English names via SUPPLEMENTS_DATABASE
```

From lib/portal/supplements-database.ts (deduplicated by Plan 01):
```typescript
export interface SupplementEntry {
  id: string;          // Pattern: "{base}-{lang}" e.g. "chamomile-es", "chamomile-en"
  name: string;
  aliases: string[];
  category: 'herb' | 'vitamin' | 'mineral' | 'amino-acid' | 'fatty-acid' | 'other' | 'condition';
  healthConditions?: string[];
  language: 'es' | 'en';
}
export const SUPPLEMENTS_DATABASE: SupplementEntry[] = [ /* ~153 entries after dedup */ ];
```

From lib/services/pubmed-search.ts (CURRENT - to be updated):
```typescript
// Only 10 entries - THIS IS THE BUG for quiz route
const SUPPLEMENT_LEXICON: Record<string, { name: string; terms: Set<string> }> = {
  TURMERIC: { name: 'Curcuma', terms: new Set(['turmeric', 'curcumin', 'curcumina']) },
  // ... only 9 more entries
};

export async function searchPubMed(condition: string): Promise<PubMedQueryResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate SUPPLEMENT_LEXICON from SUPPLEMENTS_DATABASE and update pubmed-search</name>
  <files>lib/services/lexicon-generator.ts, lib/services/lexicon-generator.test.ts, lib/services/pubmed-search.ts</files>
  <behavior>
    - Test: generateLexicon() returns a Record with keys for all unique supplement base IDs
    - Test: Each lexicon entry has a `name` (Spanish display name) and a `terms` Set with at least 2 entries
    - Test: "manzanilla" appears in the terms of a lexicon entry
    - Test: Lexicon entry count >= 75 (accounting for deduplication)
    - Test: All Spanish aliases and English aliases are merged into each supplement's terms Set
    - Test: Terms are lowercased for case-insensitive matching
  </behavior>
  <action>
    Create `lib/services/lexicon-generator.ts`:

    1. Import `SUPPLEMENTS_DATABASE` and `SupplementEntry` from `@/lib/portal/supplements-database`
    2. Create `generateLexicon(): Record<string, { name: string; terms: Set<string> }>` function that:
       - Groups supplements by base ID (strip `-es`/`-en` suffix from `entry.id`)
       - For each group, merges: entry.name (lowercased), all aliases (lowercased) from both ES and EN entries
       - Uses the Spanish `name` as the display name (since primary market is LatAm)
       - Returns a Record keyed by UPPER_SNAKE_CASE of the base ID (e.g., "ashwagandha" -> "ASHWAGANDHA")
    3. Export `generateLexicon` and also export `const GENERATED_LEXICON = generateLexicon()` for direct import

    Update `lib/services/pubmed-search.ts`:
    1. Replace the hardcoded 10-entry `SUPPLEMENT_LEXICON` with import from lexicon-generator
    2. `import { GENERATED_LEXICON } from './lexicon-generator';`
    3. `const SUPPLEMENT_LEXICON = GENERATED_LEXICON;`
    4. Keep the existing SUPPLEMENT_LEXICON type signature unchanged so all downstream code works
    5. Do NOT change any other code in pubmed-search.ts - only the LEXICON source

    IMPORTANT: The SupplementEntry `id` field follows the pattern `{name}-{lang}`. Use this to group ES/EN pairs. Some supplements may only have one language entry - handle gracefully.
  </action>
  <verify>
    <automated>cd /Users/latisnere/Documents/suplementAI && npx jest lib/services/lexicon-generator.test.ts --no-cache 2>&1 | tail -20</automated>
  </verify>
  <done>
    - lexicon-generator.ts exports generateLexicon() that produces 75+ entries from SUPPLEMENTS_DATABASE
    - pubmed-search.ts uses GENERATED_LEXICON instead of hardcoded 10-entry SUPPLEMENT_LEXICON
    - "manzanilla", "curcuma", "rhodiola", and all database supplements are covered
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaustive coverage and integration tests</name>
  <files>lib/services/__tests__/lexicon-coverage.test.ts, lib/services/__tests__/search-integration.test.ts</files>
  <behavior>
    - Test: Every SupplementEntry.name (lowercased) from SUPPLEMENTS_DATABASE exists in at least one LEXICON entry's terms Set
    - Test: Every SupplementEntry.aliases[i] (lowercased) exists in at least one LEXICON entry's terms Set
    - Test: Specific regression checks: "manzanilla", "rhodiola", "ginkgo biloba", "spirulina", "colageno"
    - Test: No empty terms Sets in any lexicon entry
    - Test: searchPubMed("manzanilla") does NOT throw
    - Test: searchPubMed with 10 representative supplements returns PubMedQueryResult shape
    - Test: searchPubMed with unknown term returns empty results (not throws)
  </behavior>
  <action>
    Create `lib/services/__tests__/lexicon-coverage.test.ts`:

    1. Import `SUPPLEMENTS_DATABASE` from `@/lib/portal/supplements-database`
    2. Import `GENERATED_LEXICON` from `@/lib/services/lexicon-generator`
    3. Flatten all lexicon terms into a single Set for efficient lookup

    Test structure:
    - `describe('Every database supplement is in the lexicon')` - use test.each with all entries
    - `describe('Every alias is in the lexicon')` - check each alias.toLowerCase()
    - `describe('Regression: previously missing supplements')` - explicit "manzanilla", "rhodiola", etc.
    - `describe('Lexicon integrity')` - no empty sets, all lowercase, entry count >= 75

    Create `lib/services/__tests__/search-integration.test.ts`:

    1. Mock `global.fetch` to simulate PubMed API responses (do NOT hit real PubMed)
    2. Import `searchPubMed` from `@/lib/services/pubmed-search`
    3. Create mock PubMed response factory with valid XML (`<WebEnv>`, `<QueryKey>`, `<Count>` tags)

    Test structure:
    - `describe('Known supplements do not error')` - test.each with 10 representative supplements
    - `describe('Unknown terms return empty results')` - "xyz_nonexistent_12345"
    - `describe('Network failures handled gracefully')` - mock fetch rejection

    IMPORTANT: The `searchPubMed` function parses PubMed XML via regex. Mock XML must include `<WebEnv>`, `<QueryKey>`, and `<Count>` tags.
  </action>
  <verify>
    <automated>cd /Users/latisnere/Documents/suplementAI && npx jest lib/services/__tests__/lexicon-coverage.test.ts lib/services/__tests__/search-integration.test.ts --no-cache --verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 158 individual supplement name checks pass (100% coverage)
    - All alias checks pass
    - Regression checks for "manzanilla" and previously missing supplements pass
    - 10 representative supplements pass search without throwing
    - Unknown terms and network failures handled gracefully
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `npx jest lib/services/lexicon-generator.test.ts --no-cache` - Lexicon generation tests pass
2. `npx jest lib/services/__tests__/lexicon-coverage.test.ts --no-cache --verbose` - All 158 supplement coverage checks pass
3. `npx jest lib/services/__tests__/search-integration.test.ts --no-cache --verbose` - Integration tests pass
4. `npx tsc --noEmit` - No TypeScript errors
5. Verify SUPPLEMENT_LEXICON has 75+ entries (was 10)
</verification>

<success_criteria>
- SUPPLEMENT_LEXICON auto-generated from SUPPLEMENTS_DATABASE with all supplements (75+ unique entries with es+en terms merged)
- "manzanilla" is in the lexicon and searchPubMed("manzanilla") does not throw
- 100% of SUPPLEMENTS_DATABASE entries have matching lexicon terms (automated proof)
- Tests serve as regression guard against future DATABASE/LEXICON desync
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-search-backend-fix/01-02-SUMMARY.md`
</output>
