---
phase: 03-categories-links-audit
plan: 03
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - app/[locale]/portal/category/[slug]/page.tsx
  - components/portal/GuidesCategories.tsx
  - app/[locale]/portal/supplement/[slug]/page.tsx
autonomous: true
requirements:
  - CAT-02
  - LINK-01
  - LINK-02
must_haves:
  truths:
    - "Category back-link points to /portal, not /portal/search"
    - "GuidesCategories View All button routes to /portal, not /portal/categories"
    - "GuidesCategories uses locale-aware useRouter from @/src/i18n/navigation"
    - "Supplement detail page back-link has null-check: unknown benefit param falls back to /portal"
    - "No component in app/ or components/ contains /portal/search or /portal/categories references"
    - "LINK-01 and LINK-02 tests all pass"
  artifacts:
    - path: "app/[locale]/portal/category/[slug]/page.tsx"
      provides: "Fixed back-link href and verified notFound() call"
      contains: "href=\"/portal\""
    - path: "components/portal/GuidesCategories.tsx"
      provides: "Fixed router.push destination + locale-aware useRouter import"
      contains: "router.push('/portal')"
    - path: "app/[locale]/portal/supplement/[slug]/page.tsx"
      provides: "Null-checked back-link using getCategoryBySlug validation"
      contains: "getCategoryBySlug"
  key_links:
    - from: "components/portal/GuidesCategories.tsx"
      to: "@/src/i18n/navigation"
      via: "useRouter import (locale-aware)"
      pattern: "from '@/src/i18n/navigation'"
    - from: "app/[locale]/portal/supplement/[slug]/page.tsx"
      to: "lib/knowledge-base.ts"
      via: "getCategoryBySlug(benefit) null-check"
      pattern: "getCategoryBySlug"
---

<objective>
Fix three hardcoded broken navigation links and add a null-check for the dynamic benefit param in the supplement detail back-link. Also fixes GuidesCategories locale routing by correcting the useRouter import source.

Purpose: Satisfies LINK-01 (all hrefs audited and functional), LINK-02 (broken link attempts show 404 / safe fallback), and CAT-02 (category pages show correct supplements without producing undefined hrefs).
Output: 3 files modified; LINK-01 and LINK-02 tests green.
</objective>

<execution_context>
@/Users/latisnere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/latisnere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-categories-links-audit/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact locations and patterns. No codebase exploration required. -->

File 1: app/[locale]/portal/category/[slug]/page.tsx
  - Line 42: href="/portal/search"  →  change to  href="/portal"
  - Already contains notFound() call — verify it exists, do not add a second one.

File 2: components/portal/GuidesCategories.tsx
  - Line 10 (approx): import { useRouter } from 'next/navigation'
    Change to: import { useRouter } from '@/src/i18n/navigation'
  - Line 240 (approx): router.push('/portal/categories')
    Change to: router.push('/portal')
  - All other router.push calls in this file (handleCategoryClick, 8 occurrences) remain unchanged — they push to /portal/results?q=... which is correct.
  - Do NOT introduce a Link element — the component is button-driven throughout.

File 3: app/[locale]/portal/supplement/[slug]/page.tsx
  - This is a 'use client' component — do NOT add notFound() here.
  - Line 186 (approx): href={`/portal/category/${benefit}`}
    Replace with Option B null-check:
      const backHref = (benefit && getCategoryBySlug(benefit))
        ? `/portal/category/${benefit}`
        : '/portal';
    Then use: href={backHref}
  - getCategoryBySlug must be imported from '@/lib/knowledge-base' (check if already imported; if not, add to import).
  - The benefit variable comes from useSearchParams() — it may already be defined nearby.

From @/src/i18n/navigation.ts (established Phase 2 pattern):
  export function useRouter(): locale-aware router  // drop-in replacement for next/navigation useRouter

From lib/knowledge-base.ts:
  export const getCategoryBySlug = (slug: string): HealthCategory | undefined => { ... }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix category page back-link (href /portal/search → /portal)</name>
  <files>app/[locale]/portal/category/[slug]/page.tsx</files>
  <action>
    Read app/[locale]/portal/category/[slug]/page.tsx. Find the back-link on or near line 42 that has href="/portal/search". Change it to href="/portal". Verify that notFound() is already present in the file (it should be — do not add a second call). Write the updated file.

    Confirm no other /portal/search references remain in this file.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/categories-links-audit.test.ts -t "LINK-01" --no-coverage 2>&1 | grep -E "(PASS|FAIL|category page)" | head -5</automated>
  </verify>
  <done>
    The category page back-link test in LINK-01 passes. File contains href="/portal" where "/portal/search" used to be. File still contains notFound() call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix GuidesCategories useRouter import and View All destination</name>
  <files>components/portal/GuidesCategories.tsx</files>
  <action>
    Read components/portal/GuidesCategories.tsx. Make exactly two changes:

    1. Change the useRouter import from:
         import { useRouter } from 'next/navigation'
       to:
         import { useRouter } from '@/src/i18n/navigation'

    2. Find the router.push('/portal/categories') call (the "View All Categories" button handler, approximately line 240). Change it to router.push('/portal').

    Do NOT modify any other router.push calls in this file (category card click handlers that push to /portal/results?q=... are correct and must stay unchanged).
    Do NOT introduce a Link element — the component is button-driven.

    Write the updated file.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/categories-links-audit.test.ts -t "LINK-01" --no-coverage 2>&1 | grep -E "(PASS|FAIL|GuidesCategories|portal/categories)" | head -5</automated>
  </verify>
  <done>
    LINK-01 GuidesCategories test passes. File has no /portal/categories reference. useRouter is imported from @/src/i18n/navigation. All other router.push calls are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add null-check for benefit param in supplement detail back-link</name>
  <files>app/[locale]/portal/supplement/[slug]/page.tsx</files>
  <action>
    Read app/[locale]/portal/supplement/[slug]/page.tsx. Locate the back-link near line 186 that uses href={`/portal/category/${benefit}`} without a guard.

    Step 1: Check if getCategoryBySlug is already imported from '@/lib/knowledge-base'. If not, add it to the existing import from that module.

    Step 2: Before the JSX that renders the back-link, derive a safe href variable:
      const backHref = (benefit && getCategoryBySlug(benefit))
        ? `/portal/category/${benefit}`
        : '/portal';

    Step 3: Replace the href attribute from href={`/portal/category/${benefit}`} to href={backHref}.

    This file is 'use client' — do NOT add notFound() here. The null-check is the correct and complete LINK-02 fix for this file.

    Write the updated file.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/categories-links-audit.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    All LINK-01 and LINK-02 tests pass. Supplement detail page file no longer contains bare href={`/portal/category/${benefit}`}. File contains backHref variable using getCategoryBySlug. No notFound() call added (client component).
  </done>
</task>

</tasks>

<verification>
Run full phase test file: npx jest lib/__tests__/categories-links-audit.test.ts --no-coverage

Expected: LINK-01 all 3 tests pass, LINK-02 both tests pass, CAT-02 test passes.

Also run wider i18n suite to confirm no regressions from the GuidesCategories import change:
npx jest lib/__tests__/categories-links-audit.test.ts lib/__tests__/i18n-routing.test.ts --no-coverage
</verification>

<success_criteria>
- app/[locale]/portal/category/[slug]/page.tsx: href="/portal" replaces "/portal/search"; notFound() present
- components/portal/GuidesCategories.tsx: useRouter from @/src/i18n/navigation; router.push('/portal') for View All; other pushes unchanged
- app/[locale]/portal/supplement/[slug]/page.tsx: backHref variable with getCategoryBySlug null-check; no bare /portal/category/${benefit}; no notFound()
- LINK-01 block: all 3 tests green
- LINK-02 block: both tests green
- CAT-02 block: test green
- No regressions in i18n-routing.test.ts
</success_criteria>

<output>
After completion, create .planning/phases/03-categories-links-audit/03-03-SUMMARY.md
</output>
