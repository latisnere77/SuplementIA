Confirmed the local CI parity harness through invariants, lint, type-check, build, Jest, and Playwright, and documented the CI-only gaps for `npm audit` and `RUN_REAL_SEARCHES=1`. `TASKS.md` now records the local CI parity task as DONE.
