Extended `scripts/gsd/tool-budget-policy.mjs` with physical per-session `LimitToolCounts` for exec, git, and apply_patch, including hard maxima, stricter-only env overrides, and hook wiring for Bash plus edit tools. The exact harness, fan-out remediation, and full `GSD_DONE: PASS` completed for the limit-count task.
