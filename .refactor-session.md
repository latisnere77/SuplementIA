Implemented `scripts/gsd/tool-budget-policy.mjs` as a local PreToolUse DebounceHook with per-session command counting, a hard max of two identical normalized commands, fail-closed missing-session behavior, and temp-dir state. The focused harness, invariants, fan-out remediation, and DONE oracle completed for the debounce task.
