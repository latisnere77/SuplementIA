Debug/test routes were inventoried and classified without changing runtime behavior; `/api/test-*`, `/api/portal/test-config`, `debug-enrich`, and `stream-test` are now documented as remove/protect candidates. The Playwright portal harness passed with 46/46 tests.
